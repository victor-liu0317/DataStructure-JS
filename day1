数组
var days = ['Monday','Tuesday','wednesday']
求斐波那契数列前20位：
  var fibonacci = [];
  fibonacci[0] = 1;
  fibonacci[1] = 1;
  for(var i = 2;i < 20;i++){
    fibonacci[i] = fibonacci[i-1] + fibonacci[i-2]
  }
  console.log(fibonacci);
push()方法，元素添加到数组末尾，任意个
unshift()方法，直接把数值插入数组首位
pop()方法，删除数组最靠后的元素
shift()方法，删除数组第一个元素
splice()方法，通过指定索引，删除或插入相应位置和数量的元素
  numbers.splice(5,3);  //删除从索引5开始的3个元素，包括5
  numbers.splice(5,0,2,3,4);  //第一个参数：想要插入或删除的元素的索引值，
                              //第二个：删除元素的个数(这里是插入，传入0)，第三个及以后为添加的元素
concat()方法，按照传入参数顺序连接指定数组
迭代器函数：every()、some()、map()、filter()、reduce()
reverse()方法，数组反序
sort()方法，对数组做排序，把元素默认成字符串进行排序
  numbers.sort(function(a,b){
    return a-b;
  } //根据返回值排序
indexOf()方法、lastIndexOf()方法，分别返回与参数匹配的第一个和最后一个元素的索引
toString()方法，把所有元素输出为一个字符串
join()方法，用一个分隔符把元素隔开
  var numbersString = numbers.join('-');
